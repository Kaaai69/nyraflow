// Профиль студии для системного промпта.
//
// Осознанная копия ключевых фактов из content/home.ts лендинга: мини-апп —
// отдельное приложение со своей сборкой, импортировать через границу проекта
// нельзя. Здесь только то, что реально влияет на разбор брифа: форматы работы,
// цены и этапы. При изменении прайса на лендинге правится и здесь.

export type StudioProduct = {
  id: "landing" | "web_service" | "ai_automation";
  title: string;
  priceFrom: number;
  whenItFits: string;
  /** Типичный срок сборки. Нужен, чтобы модель не пугала клиента сроками. */
  typicalTerm: string;
};

export const studioProducts: readonly StudioProduct[] = [
  {
    id: "landing",
    title: "Лендинг",
    priceFrom: 30_000,
    whenItFits:
      "запуск продукта или услуги, проверка новой ниши, текущий сайт не ведёт к заявке",
    typicalTerm: "около двух недель",
  },
  {
    id: "web_service",
    title: "Веб-сервис или Telegram Mini App",
    priceFrom: 60_000,
    whenItFits:
      "личные кабинеты, каталоги, бронирование, расчёты, внутренние инструменты — процесс уже не помещается в лендинг или таблицу",
    typicalTerm: "от трёх недель",
  },
  {
    id: "ai_automation",
    title: "Сайт с AI-автоматизацией",
    priceFrom: 120_000,
    whenItFits:
      "заявки, документы или повторяющиеся операции требуют постоянной ручной обработки",
    typicalTerm: "от четырёх недель",
  },
] as const;

export const studioStages: readonly string[] = [
  "Диагностика задачи",
  "Структура и смыслы",
  "Дизайн-концепт",
  "Разработка и интеграции",
  "Запуск и рост",
] as const;

/** Чего студия не делает — модель обязана это учитывать. */
export const studioConstraints: readonly string[] = [
  "Не работаем на конструкторах и готовых шаблонах — только собственная разработка",
  "Не берём проекты без зафиксированной бизнес-цели",
  "Не обещаем результат за счёт рекламного бюджета — отвечаем за продукт, а не за закупку трафика",
  "Не занимаемся A/B-тестированием и оптимизацией конверсии как отдельной услугой: собираем продукт и ведём его после запуска, а проверка гипотез на потоке — работа команды клиента",
] as const;
